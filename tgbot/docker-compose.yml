version: '3.9'

services:
  bot:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/bot
    depends_on:
      - db
      - redis
    networks:
      - test-mission_default
    ports:
     - "2222:2222"

networks:
  test-mission_default:
    external: true
